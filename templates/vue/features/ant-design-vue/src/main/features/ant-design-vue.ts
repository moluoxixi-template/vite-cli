/**
 * Ant Design Vue UI 组件库 feature 设置
 */

import type { UserModule } from '@/types'



/**
 * 设置 Ant Design Vue
 * @param ctx - Vue 应用上下文（未使用，自动导入）
 */
export const install: UserModule = (_ctx) => {
  // Ant Design Vue 通过 unplugin-vue-components 自动导入
}
